name: Build and Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      version:
        description: 'Version tag (e.g., v1.3.0)'
        required: true
        default: 'v1.3.0'

jobs:
  build:
    runs-on: windows-latest
    
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
        with:
          submodules: recursive

      - name: Setup MSBuild
        uses: microsoft/setup-msbuild@6fb02220983dee41ce7ae257b6f4d8f9bf5ed4ce # v2.0.0

      - name: Download BakkesMod SDK
        run: |
          git clone https://github.com/bakkesmodorg/BakkesModSDK.git ${{ github.workspace }}/bakkesmodsdk

      - name: Build Plugin
        working-directory: RankedEssentials
        run: msbuild BakkesPluginTemplate.vcxproj /p:Configuration=Release /p:Platform=x64 /p:BakkesModPath="${{ github.workspace }}"

      - name: Get version
        id: version
        shell: bash
        run: |
          if [ "${{ github.event_name }}" == "workflow_dispatch" ]; then
            echo "VERSION=${{ github.event.inputs.version }}" >> $GITHUB_OUTPUT
          else
            echo "VERSION=${GITHUB_REF#refs/tags/}" >> $GITHUB_OUTPUT
          fi

      - name: Create Release
        uses: softprops/action-gh-release@a06a81a03ee405af7f2048a818ed3f03bbf83c7b # v2
        with:
          tag_name: ${{ steps.version.outputs.VERSION }}
          name: Ranked Essentials ${{ steps.version.outputs.VERSION }}
          body: |
            ## Ranked Essentials ${{ steps.version.outputs.VERSION }}
            
            Auto-queue, auto-freeplay, and auto-skip replays for Rocket League.
            
            ### Installation
            1. Download `RankedEssentials.dll` below
            2. Copy to `%APPDATA%\bakkesmod\bakkesmod\plugins\`
            3. Load in-game: F6 → `plugin load RankedEssentials`
          files: RankedEssentials/plugins/RankedEssentials.dll
          draft: false
          prerelease: false
